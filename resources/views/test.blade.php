{{-- <table border="1">
    <thead>
        <tr>
            <th>Nama</th>
        </tr>
    </thead>
    <tbody>
        @foreach ($programServices as $service)
            <tr>
                <td>{{ $service->name }}</td>
            </tr>
        @endforeach
    </tbody>
</table>

<div>
    {{ $programServices->links() }}
</div> --}}
